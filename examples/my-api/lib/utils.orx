-- my-api/lib/utils.orx — shared utilities

fn clamp(val, min_val, max_val) {
    if val < min_val { return min_val }
    if val > max_val { return max_val }
    return val
}

fn is_empty(s) {
    return len(s) == 0
}
